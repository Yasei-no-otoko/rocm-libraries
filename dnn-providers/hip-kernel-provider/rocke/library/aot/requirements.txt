# rocke AOT producer: build-time Python dependencies.
#
# The AOT build tool (rocke_aot_build.py) and the CTest-driven Python suites
# run under ${ROCKE_PYENV_PYTHON} — the build-local editable venv from
# rocke/CMakeLists.txt. That venv installs `platform` with deps but `library`
# with --no-deps, so these third-party packages cannot ride on
# library/pyproject.toml. They are installed here via a
# `pip install -r library/aot/requirements.txt` line in the rocke-pyenv step.

# Formal JSON Schema validation for instance / sidecar schemas.
jsonschema>=4.20

# Numeric verifier and generated-artifact smoke tests. (Also a platform dep, but
# the AOT tree owns its own build-time environment, so declare it explicitly.)
numpy>=1.24

# CTest-driven Python unit tests + the --fail-under=90 coverage gate.
pytest>=7.0
pytest-cov>=4.0
